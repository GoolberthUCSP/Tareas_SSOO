Se solicitó crear un bucle de 100 iteraciones.
El comando para realizar los bucles se encuentra en:
    - loop.sh

Pasos para su ejecución:
    - Ejecutar ./send
    - Ejecutar loop.sh pid_send
        #Se le pasará como parámetro el ID del proceso send

Luego se terminará al proceso.